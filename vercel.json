{
  "ignoreCommand": "changed=$(git diff --name-only HEAD^ HEAD 2>/dev/null); [ -z \"$changed\" ] && exit 1; echo \"$changed\" | grep -qv '^CHANGELOG.md$' && exit 1; exit 0",
  "crons": [
    {
      "path": "/api/cron/discussion-digest",
      "schedule": "0 9 * * 1"
    },
    {
      "path": "/api/cron/expire-members",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/nudge-pending-members",
      "schedule": "0 10 * * 3"
    }
  ]
}
